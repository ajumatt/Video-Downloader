# Copy this file to .env and fill in real values.
# NEVER commit .env — only this .env.example (placeholders only) belongs in version control.
# See SECURITY.md for credential handling rules.

# Database
DATABASE_URL=

# Auth / API keys
API_KEY=
SESSION_SECRET=

# App config
PORT=3000
NODE_ENV=development
